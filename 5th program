#Hamming distance is the number of characters that differns between two strings.
def hammingDist(str1, str2):
    """function that computes the hamming distance between two strings"""
    i = 0
    count = 0

    while (i < len(str1)):
        if (str1[i] != str2[i]):
            count += 1
        i += 1
    return count
print(hammingDist("jayesh","jayesh"))
